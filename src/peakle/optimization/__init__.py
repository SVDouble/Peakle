"""DEPRECATED (v1): superseded by peakle.localize (ray-cast HorizonProfile solver, calibrated verdicts). Kept only because the alignment-lab web app's Solve data model still wires into it. Do not build new code on this.

Camera pose optimization."""
