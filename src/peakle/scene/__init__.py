"""In-memory scene composition for the live web server.

This package holds the synthetic scene in memory and computes camera views on
the fly, in contrast to ``peakle.demo`` which composes a reproducible run that
writes artifacts to disk.
"""
